\documentclass[12pt]{article}

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{algorithmic}
\usepackage{enumerate}

%Gummi|065|=)
\title{An algorithm to be analyzed}
%\author{Alexander van der Mey \and 
%		Wei-Ning Huang \and
%		Dion Timmermann}

\date{}
\begin{document}

\maketitle

\newtheorem{lemma}{Lemma}

\section{A sketch of proof of the key lemma}


Let $D^*$ be a smallest dominating set of $G=(V,E)$.

\begin{lemma}
%During each iteration of the loop in lines~2--5 of $A$,
Whenever line~3 of $A$ is executed,
there exists $u\in D^*\setminus {\cal D}$ satisfying
$$
\left|N[u]\cap \left(V\setminus \bigcup_{v\in {\cal D}}\,N[v]\right)\right|
\ge \frac{1}{|D^*|}
\cdot\left|V\setminus \bigcup_{v\in {\cal D}}\,N[v]\right|
$$
and that $N(u)\not\subseteq {\cal D}$.
\end{lemma}
\begin{proof}[Sketch of proof.]
Denote the elements of $D^*$ by
$v_1$, $v_2$, $\ldots$, $v_\ell$.
As $D^*$ is a dominating set,
$$
\bigcup_{i=1}^\ell\, N[v_\ell]=V.
$$
Consequently,
$$
V\setminus \bigcup_{v\in {\cal D}}\,N[v]
\subseteq
\bigcup_{i=1}^\ell\, N[v_\ell].
$$
Use this to show the existence of $j\in \{1,2,
\ldots,\ell\}$ satisfying
$$N[v_j]\cap \left(V\setminus \bigcup_{v\in {\cal D}}\,N[v]\right)
\ge \frac{1}{\ell}
\cdot\left|V\setminus \bigcup_{v\in {\cal D}}\,N[v]\right|.$$
\end{proof}






\end{document}
